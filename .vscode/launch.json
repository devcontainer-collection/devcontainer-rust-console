{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/app/build/debug/debug/main",
            "args": [],
            // "cwd": "${workspaceFolder}/app/src", // Working directory for the program
            "preLaunchTask": "build for debugging",
            "stopOnEntry": false,
            "console": "integratedTerminal",
            "terminal": "integrated",
        }
    ]
}